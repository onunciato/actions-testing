name: Handle pull requests
on:
  push:
    branches:
      - master
jobs:    
  run_pr_checks:
    name: Run all checks
    runs-on: self-hosted
    steps:
      - name: Try to use secrets
        run: |
          echo "$SOME_SECRET"
        env:
          SOME_SECRET: ${{ secrets.SOME_SECRET }}
