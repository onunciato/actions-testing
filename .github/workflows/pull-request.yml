name: Run PR checks
on:
  pull_request:
    branches:
      - master
  issue_comment:
    types:
      - created
jobs:
  dispatch:
    if: contains(github.event.comment.body, '/run-checks')
    name: Handle PR Comment
    runs-on: ubuntu-latest
    steps:
      - name: Run PR checks
        run: |
          echo "Hey, I ran!"
